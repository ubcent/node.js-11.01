const task = require('./task');

//task.add({id: null, content: 'turn off TV', status: 'pending', priority: 0});

//task.update([{content: 'Go shopping'}, 3]);

//task.complete(4);

//task.prioritize(3);

